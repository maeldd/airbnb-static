<div class="container text-center padded">
  <ul class="list-inline text-center">
    <% ["pierre", "mael"].each do |member| %>
      <li>
        <%= image_tag "#{member}.jpeg", class: "avatar-extra-large" %>
        <h3><%= member.capitalize %></h3>
      </li>
    <% end %>
  </ul>
</div>
